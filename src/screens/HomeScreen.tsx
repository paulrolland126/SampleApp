import React from "react";
import {SafeAreaView} from "react-native-safe-area-context";

const HomeScreen = () => {
  return (
    <SafeAreaView>
      {/**
       * 1. read data from assets/list.json
       * 2. list the items
       * 3. add feature to add item to the list
       * 4. user should be able to edit item name by long press on it and bu hitting return key on keyboard
       * 5. implement DetailedView with item name as textInput
       */}
    </SafeAreaView>
  );
};

export default HomeScreen;
